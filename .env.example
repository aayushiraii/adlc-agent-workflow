# JWT Configuration
# Secret key used to sign JWT tokens — replace with a strong random string in production
JWT_SECRET=your_super_secret_key_here

# Token expiry duration — examples: 1h, 30m, 7d, 86400 (seconds)
JWT_EXPIRY=1h

# Server Configuration
PORT=3000
